// Atomic Design - Molecules
// Small groups of atoms forming a single functional unit
// (search box, form field with label, social-share row, language switcher).
// Stateless or with minimal local UI state.
export {};
