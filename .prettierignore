node_modules
dist
.output
.vinxi
coverage
pnpm-lock.yaml
package-lock.json
bun.lock
routeTree.gen.ts
src/lib/icons/lucideIconNodes.generated.ts
# Snapshot bramek autoryzacji generowany z supabase/migrations przez
# scripts/generate-authz-snapshot.ts - jedna bramka na linię, żeby diff pokazywał
# dokładnie tę, która zmieniła zbiór ról. Prettier rozbiłby to na setki linii i
# rozjechał porównanie bajt w bajt w bramce parytetu.
src/lib/authz/authzSnapshot.generated.ts
# Metadane sandboxu platformy (manifest MCP + plan) - przepisywane przy
# każdym buildzie, więc formatowanie ich to tylko szum w diffie. Ten sam powód,
# dla którego eslint.config.js ignoruje src/routes/mcp.ts i list-tools.ts.
.lovable
# Generowane przez wtyczkę MCP platformy (ignorowane też przez ESLint).
src/routes/mcp.ts
src/routes/**/list-tools.ts
src/routes/**/invoke-tool/*.ts
src/routes/**/oauth-protected-resource.ts
# Typy generowane z bazy (`supabase gen types typescript`). ESLint pomija ten
# plik od zawsze, Prettier NIE - i przez tę asymetrię `prettier --check .` nie
# dawał się wpiąć w CI jako bramka. Formatowanie 20 tysięcy generowanych linii
# i tak przepada przy najbliższej regeneracji, więc byłby to szum wracający
# w każdym wydaniu. Ta sama zasada, co dla plików wyżej.
src/integrations/supabase/types.ts
# Artefakty Lighthouse CI (`lhr-*.json/html`, `assertion-results.json`) - wynik
# przebiegu narzędzia, nie kod źródłowy. Zacommitowane jako materiał dowodowy
# pomiaru, przepisywane przy każdym uruchomieniu.
.lighthouseci
# Audyt z 05.08 cytuje w bloku kodu DOSŁOWNY bajt NUL - to materiał dowodowy
# znaleziska (separator wpisany bajtowo na offsecie 4440), a nie zapis `<0x00>`.
# Prettier nie przepuszcza NUL przez parser i podmienia go na U+FFFD, czyli
# kasuje dokładnie tę jedną rzecz, dla której ten fragment istnieje. Dlatego
# plik zostaje poza formatowaniem - jak pozostałe pliki, których treść musi
# przetrwać bajt w bajt.
docs/AUDYT_BRUTALNY_REWIZJA_ZALOZEN_2026-08-05.md
# Raporty bramek: `reports/*` jest w .gitignore poza dwoma plikami odniesienia,
# które generują skrypty CI (`check:bundle`, `check:i18n-parity`). Formatowanie
# ich rozjechałoby porównanie z artefaktem świeżo wyprodukowanym w przebiegu.
reports
