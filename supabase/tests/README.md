# Testy bazy danych (pgTAP)

Testy SQL warstwy danych Supabase - RLS (izolacja tenantów), przypięcie
`profiles.tenant_id` oraz pełnotekstowe wyszukiwanie. Dopełniają testy
jednostkowe Vitest (`bun run test`), które nie dotykają realnego Postgresa
ani polityk RLS.

## Pliki

| Plik                                   | Co weryfikuje                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `rls_tenant_isolation_test.sql`        | „user tenanta A nie czyta postów B" (RLS na `public.posts`, szkice i opublikowane) oraz „UPDATE `tenant_id` jest ignorowany" (trigger `profiles_pin_tenant`).                                                                                                                                                                                                                                                                                                                                                                                                  |
| `search_tsquery_test.sql`              | `public.nes_search_tsquery` - unaccent + lower, prefiks `:*`, łączenie AND, sanityzacja znaków, puste/NULL → `NULL`.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `search_posts_smoke_test.sql`          | Smoke RPC `public.search_posts`: zwraca tylko opublikowane, nieusunięte posty tenanta publicznego; pomija szkice, usunięte i obcych tenantów.                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pii_column_grants_test.sql`           | Bramka CI grantów PII: `has_column_privilege` dowodzi, że `anon`/`authenticated` NIE czytają wrażliwych kolumn (`profiles.email/prefs`, `newsletter_subscribers.email`, `billing_profiles.email/tax_id/phone`, `crm_leads.email/phone`, `contact_messages.email/phone`, `author_profiles.phone/contact_email/media_contact_email/media_contact_phone`); regresja grantu = błąd buildu.                                                                                                                                                                          |
| `author_contact_privacy_test.sql`      | Prywatność kontaktów autora (finding „author contact details exposed by default", migracja 20260730120000): widok `author_profiles_public` bez ŻADNEJ kolumny kontaktowej (zostaje tylko `media_contact_name`), `get_expert_hub()` bez `contact_email` w payloadzie, `is_public` DEFAULT `false`, `admin_get_author_profile()` (SECURITY DEFINER) czyta pełny wiersz wyłącznie dla admina tego samego tenanta (nie-admin i admin obcego tenanta - pusty zbiór, anon bez EXECUTE), właściciel przez `get_own_author_profile()`, anon widzi publiczną projekcję. |
| `author_profiles_owner_tenant_scope_test.sql` | Izolacja tenanta w politykach WŁAŚCICIELA na `author_profiles` (migracja 20260803140000): polityki SELECT i DELETE zostały przy gołym `auth.uid() = user_id`, choć bliźniacze INSERT/UPDATE dostały `tenant_id = current_tenant_id()` (20260721105920) - wiersz był ZAPISYWALNY tylko w tenancie domowym, ale CZYTELNY i KASOWALNY z dowolnego kontekstu tenanta. Plik przybija: wszystkie cztery ścieżki właściciela wiążą tenanta (`pg_policies`), `get_own_author_profile()` filtruje po `current_tenant_id()` w CIELE (SECURITY DEFINER omija RLS, więc bez tego poprawka polityki jest martwą literą), zdublowany indeks `author_profiles_user_idx` usunięty przy żywym UNIQUE na `user_id` (upsert `onConflict`), oraz zachowanie: konto po dryfie (profil w tenancie B, wiersz w A) nie widzi wiersza ani przez tabelę, ani przez RPC i nie kasuje go, a właściciel zgodny z tenantem nadal czyta PII, upsertuje (ścieżka `AuthorProfileEditor`) i kasuje swój wiersz. |
| `author_profiles_public_read_view_only_test.sql` | Publiczny odczyt `author_profiles` wyłącznie przez widok (migracja 20260817120000, finding „publiczne polityki SELECT zwracają pełny wiersz - z `phone`, `contact_email`, `media_contact_email`, `media_contact_phone` - każdemu przy `is_public = true"`): obu polityk „Public/Authenticated can view public author profiles" NIE MA, `anon` nie ma na tabeli bazowej żadnej polityki ani żadnego grantu (SELECT = 42501 na grantach, nie pusty zbiór; DML też odcięty), `authenticated` wciąż bez kolumn kontaktowych, ale z kanarkiem kolumn bezpiecznych (`user_id`/`is_public` - bez nich wywraca się upsert właściciela i wewnętrzna baza ekspertów), widok `author_profiles_public` pozostaje jedyną publiczną projekcją (grant dla obu ról; anon i zalogowany czytelnik widzą publiczny profil przez widok), wiersz tabeli bazowej czytają wyłącznie właściciel i admin tenanta, a izolacja najemców trzyma na obu powierzchniach (admin obcego tenanta nie czyta wiersza przez tabelę, anon na domenie obcego tenanta nie widzi profilu przez widok). |
| `expert_request_single_generation_test.sql` | „Zapytanie do eksperta” po scaleniu dwóch generacji nazw (migracja 20260806160000): dokładnie JEDNA relacja fizyczna i komplet 10 RPC (5 nazw wołanych przez klienta + 5 domenowych) - regresja klasy 42P01/PGRST202, która zabijała funkcję na każdej świeżej bazie; pula = `GREATEST(features.expert_request_quota, dawne chat_inmail_quota_2/5)`; pętla obejścia „wyślij → anuluj → wyślij” zamknięta (anulowane NADAL liczy się do puli - i przez RPC, i przez PATCH z Data API); parytet delegatu z funkcją domenową; maszyna stanów (wycofać wolno tylko `pending`); guard kolumnowy (nadawca nie podrabia wyniku ani stempla, ale wycofanie przez RPC przechodzi); granica obszaru roboczego (odbiorca obcego tenanta niedostępny, dryf tenanta odcina skrzynkę i domknięcie, moderacja zawężona do tenanta admina); antyspam 5/24 h per odbiorca dla warstw „bezpośrednich”; ACL (anon bez EXECUTE na żadnym z 10 RPC). |
| `anonymous_insert_lockdown_test.sql`   | Bramka anty-regresyjna dla 4 findings ze skanu anonimowych INSERT-ów (2026-07-30, migracje 20260730130000 + 20260730140000): `contact_messages`, `crm_consent_log`, `related_post_clicks` i `builder_experiment_events` bez ŻADNEJ polityki INSERT i bez grantów INSERT dla `anon`/`authenticated` (INSERT z obu sesji = 42501, także z poprawnym tenantem i eksperymentem `running`); anon bez SELECT na logu zgód; service_role zachowuje zapis (ścieżki `submitContactMessage` / beacony `/api/public/related-click` i `/api/public/experiment-event`), a trigger `contact_messages_to_lead` nadal dopisuje zgodę RODO do `crm_consent_log` przy realnym zgłoszeniu. |
| `chat_privacy_isolation_test.sql`      | Prywatność czatu per tenant: legacy członkostwo cross-tenant nie daje odczytu wiadomości/konwersacji/uczestników ANI załącznika w storage; purge obiektu storage przy „cofnij wysłanie"; pin `user_id`/`tenant_id` w `notification_preferences`; wzajemne potwierdzenia odczytu (RLS na uczestnikach); `allow_messages_from` (nowe konwersacje + tryb cichy); `get_chat_peers` bez wycieku poza tenant; komplet polityk Realtime Authorization i parser topiców.                                                                                               |
| `get_chat_peers_contract_test.sql`     | Pełny kontrakt prywatności RPC `get_chat_peers` - bramka anty-regresyjna dla wycieku cross-tenant z 21.07 (DROP/CREATE dla kolumny `slug` zdmuchnął hardening z 12.07; przywrócenie: migracja 20260731213000). Samowystarczalny seed bez RPC czatu: filtr tenanta dla OBU gałęzi widoczności (discoverable oraz wspólna - także LEGACY cross-tenant - konwersacja), self zawsze widoczny ze `slug` w zwrotce, nie-discoverable bez wspólnej konwersacji ukryty w tenancie, limit wejścia 1-200 (pusta i >200-elementowa tablica odrzucone), guard `auth.uid()`, ACL (`anon` bez EXECUTE, `authenticated`/`service_role` z EXECUTE).                                        |
| `chat_whatsapp_features_test.sql`      | Funkcje czatu klasy WhatsApp: potwierdzenia dostarczenia (`mark_conversations_delivered`), pin/archiwum/wyciszenie (self-row RPC; fan-out powiadomień pomija wyciszonych), „wyczyść czat u siebie" (asymetria stron), znikające wiadomości (whitelist TTL, stampowanie `expires_at`, RLS ukrywa wygasłe, twardy purge z kasacją obiektu storage), głosówki (`kind='audio'`, limit czasu, allowlist bucketu), prywatne gwiazdki, harmonogram pg_cron.                                                                                                           |
| `community_membership_badges_test.sql` | Warstwy członkostwa i odznaki: seed 14 warstw katalogu (v3 + Partner Biznesowy) dla nowego tenanta (trigger), public read tylko publiczny tenant/aktywne, zapis admin-only we własnym tenancie, rozstrzyganie warstwy (subskrypcja→plan→tier; fallback reader), `has_tier_rank`/`has_tier_feature`, odznaki admin-only + stempel `granted_by` + powiadomienie.                                                                                                                                                                                                                                    |
| `community_events_test.sql`            | Wydarzenia: public read (published, publiczny tenant), granty kolumnowe `join_url`/`recording_url`, `rsvp_event` (wyścig o ostatnie miejsce pod `FOR UPDATE` ⇒ lista rezerwowa, idempotencja, zwalnianie miejsc, `interested` bez miejsca), bramki warstw (members ⇒ rank≥1; briefing ⇒ FLAGA `pro_briefings`), zakaz zapisu bezpośredniego (zalążkowe polityki usunięte), `get_event_access` (auth/tier/rsvp/ok) i liczniki RSVP.                                                                                                                             |
| `community_events_member_read_tier_gate_test.sql` | Bramka warstwy w polityce SELECT `events member read` (migracja 20260817220000, finding „każdy zalogowany czytał pełny wiersz wydarzeń bramkowanych warstwą"): polityka pytała wyłącznie o `status='published'` i tenanta, więc darmowe konto (rank 0) pobierało `location`/`capacity`/`ticket_price_cents` wydarzeń members i tier-gated jednym GET-em, omijając bramkę, którą `rsvp_event`/`get_event_access` egzekwują od zawsze. Plik przybija: strukturalnie (pg_policies) polityka pyta o `visibility` ORAZ warstwę (`current_tier_rank` + flaga `pro_briefings`); reader (rank 0) widzi dokładnie to co anon; member (rank 10) widzi members i public z progiem, ale NIE members-briefing (decyduje FLAGA, nie ranga); pro widzi też briefing; redakcja bez subskrypcji czyta cały tenant ze szkicem włącznie (osobna polityka „events staff read" nietknięta); `get_event_access` dla niekwalifikującego się konta nadal zwraca `tier_required` (kontrakt RPC bez zmian). |
| `community_events_waitlist_test.sql`   | Lista rezerwowa wydarzeń: komplet ⇒ `waitlist` FIFO (stabilna pozycja przy ponowieniach), rezygnacja z `going`/podniesienie `capacity` awansuje czoło kolejki + powiadomienie, `get_event_rsvp_counts.waitlist`, `get_event_waitlist_position`, `get_event_access` reason=`waitlisted`; nagrania za FLAGĄ `recordings` (URL nie wycieka bez uprawnienia, `watch_reason` steruje upsellem).                                                                                                                                                                     |
| `community_qa_summary_test.sql`        | Podsumowanie sesji Q&A jako treść: bramki (staff/host; sesja `answering`/`closed`; min. 1 odpowiedź), workflow redakcyjny publikacji (`can_publish_content` - host kompiluje szkic, publikuje admin), escaping HTML treści pytań, anonimowość Chatham House, porządek głosów, idempotentny upsert przez `qa_sessions.post_id`, powiadomienia autorów przy pierwszej publikacji, publikacja jednokierunkowa.                                                                                                                                                    |
| `community_reputation_test.sql`        | Reputacja + tablica kontrybutorów: wagi punktów z istniejących danych (Q&A/odznaki, breakdown per źródło), tablica wyłącznie dla zalogowanych i wyłącznie profile `discoverable=true` bez kont redakcyjnych, `get_my_reputation` (własny wynik bez opt-in, pozycja tylko dla widocznych).                                                                                                                                                                                                                                                                      |
| `profile_badge_domain_sync_test.sql`   | Kanoniczne 4 odznaki, administracyjne RPC bez klientowego `tenant_id`, blokada bezpośredniego DML, guard relacji profil-tenant, pochodzenie nadania, eventy `profile_badge.granted/revoked.v1`, automatyczny `contributor` przy 150 pkt aktywności w 90 dni i brak automatyzacji sygnałów zaufania. |
| `profiles_verification_guard_test.sql` | Bramka pól `verified_at`/`verified_by` (migracja 20260806130000, regres z 20260806094104): zbiór uprawnionych = `admin` + `super_admin` (`super_admin` BEZ osobnej roli `admin` realnie zapisuje - to właśnie zawężenie, które weszło poza PR-em), odmowa jako `42501` z komunikatem bramki (`editor` przechodzi bliźniaczy `profiles_guard_privileged_columns`, ale nie ten), brak efektu zamiast wyjątku dla zwykłego użytkownika (kolejność triggerów: bliźniak po cichu pinuje pola), sankcjonowane furtki (`app.verification_sync`, brak `auth.uid()` dla `service_role`/cron), pokrycie INSERT (self-insert z `verified_at` odrzucony - polityka "Users insert own profile" pozwala wstawić własny wiersz) oraz parytet RPC panelu `admin_set_profile_verification`. |
| `profiles_public_anon_gate_test.sql`   | Bramka widoczności widoku `profiles_public` (migracja 20260806160000, finding „22-kolumnowa projekcja każdego profilu dla anon"): widok był definerowy z `GRANT SELECT` dla `anon` i zawężał WYŁĄCZNIE po `tenant_id = public_tenant_id()`, więc goły profil członka (imię, nazwisko, avatar, okładka, dwa bio, stanowisko, firma, specjalizacja, sześć linków social) wychodził anonimowi jednym GET-em, a jedyną mitygacją było `noindex` na /author/$slug - czyli prośba do crawlera, nie kontrola dostępu. Plik przybija dwie ADDYTYWNE warstwy: PUBLICZNA wymaga realnej publicznej obecności (rola redakcyjna / odznaka `expert` / publiczny profil autorski / opublikowana treść) - goły członek ORAZ członek z samym `discoverable` (opt-in WEWNĘTRZNY) wypadają, a anon widzi dokładnie 4 z 8 sylwetek; CZŁONKOWSKA (tylko zalogowany) idzie za tenantem DOMOWYM (`current_tenant_id()`, nie za nagłówkiem) i daje własny wiersz, `discoverable`, staff tenanta oraz zaakceptowany kontakt. Osobno: podmiana `x-tenant-host` na domenę obcego tenanta NIE otwiera jego katalogu osobowego (widać tylko to, co ten tenant publikuje), ACL helperów bramki (`anon` z EXECUTE na `profile_has_public_presence`/`caller_is_tenant_staff`/`caller_is_connected_to`, bez EXECUTE na `get_my_public_exposure`) oraz powody ekspozycji zwracane przez `get_my_public_exposure()` dla własnego wiersza. |
| `community_qa_test.sql`                | Q&A: sesje bez szkiców, grant kolumnowy `user_id` (Chatham House), `ask_qa_question` (sesja `open`, rate limit 5/h, `author_display` = nazwa profilu - nigdy e-mail, anonimowość, powiadomienie hosta), zakaz INSERT-u bezpośredniego, głosy tylko na approved/answered, `list_qa_questions` (priorytet Pro `qa_priority` > głosy > starszeństwo), moderacja hosta + stempel odpowiedzi.                                                                                                                                                                       |
| `community_polls_contrib_test.sql`     | Ankiety + kontrybutorzy: public read bez szkiców, `vote_poll` (walidacja opcji, zamknięte/przeterminowane, upsert zmiany głosu), anti-anchoring `get_poll_results`/`_bulk`, zakaz zapisu bezpośredniego do `poll_votes`, zgłoszenia (own insert `submitted`, rate limit 3/24h, izolacja own/staff per tenant, akceptacja ⇒ odznaka `contributor`).                                                                                                                                                                                                             |
| `community_tracker_test.sql`           | Tracker UE: public read (published, publiczny tenant), aktualizacje publiczne tylko przy opublikowanym dossier, obserwowanie owner-only za warstwą z flagą `regulatory_monitoring` (`WITH CHECK` published + bramka Pro), licznik obserwujących przez RPC, trigger zmiany etapu (przestawia dossier, stempluje `stage_from`, alarmuje obserwujących).                                                                                                                                                                                                                                                                  |
| `community_groups_test.sql`            | Kręgi (rozmowy grupowe): walidacja tytułu, serwerowy filtr kandydatów (blokady, `allow_messages_from`), owner-only dopraszanie/rename, fan-out N>2 (`unread_count` dla każdego oprócz nadawcy), guard grupowy (tryb cichy nie knebluje kręgu, blokada pary obowiązuje), wyjście ownera z przekazaniem własności i kasacja pustego kręgu.                                                                                                                                                                                                                       |
| `push_and_digest_test.sql`             | Kanoniczny potok push/digest: own-row RLS subskrypcji, kolejka `notification_push_queue` service-role-only, trigger kolejkuje tylko przy opt-in `push_enabled` + żywej subskrypcji, `claim_push_jobs` (atomowy claim z backoffem), `claim_due_digests` (stempel `digest_last_sent_at`, pomija przeczytane).                                                                                                                                                                                                                                                    |
| `notification_preferences_gating_test.sql` | Bramkowanie powiadomień per rodzaj w `enqueue_notification` (40 asercji): każdy z 10 przełączalnych rodzajów włączony ⇒ wstawia / wyłączony ⇒ pomija, `security` always-on; parytet strukturalny katalog `notifications_kind_check` ⇄ kolumny `enabled_<rodzaj>` (w obie strony); **sweep sterowany katalogiem** - nowy rodzaj bez gałęzi w CASE (`ELSE true` ⇒ przeciek) albo bez kolumny wywala test bez dopisywania asercji; bramka czyta preferencje ODBIORCY, nie `auth.uid()` wołającego; fail-open przy braku wiersza preferencji; stempel `tenant_id` z profilu odbiorcy przy wołającym z obcego tenanta + RLS (obcy tenant nie czyta, odbiorca czyta); ACL producenta (anon/authenticated bez EXECUTE, service_role z EXECUTE - migracja 20260803090000); odporność na wywołanie z triggera (NULL/pusty/nieznany rodzaj ⇒ NULL zamiast wyjątku) i dedup 5 min po `(user, kind, href)`. |
| `expert_tenant_scope_notifications_test.sql` | Izolacja tenanta w predykatach „ekspert"/„VIP" (migracja 20260806160000) i doręczenia modułu „Zapytanie do eksperta" (20260806161000). Predykaty: wszystkie cztery źródła statusu (`author_profiles`, `event_speakers` przez `events.tenant_id`, role redakcyjne, granty członkowskie) rozstrzygane WYŁĄCZNIE w podanym obszarze roboczym - autor/VIP obszaru A nie jest ekspertem w B, wariant 1-arg rozstrzyga obszar WYWOŁANIA, `NULL` tenant = brak statusu, ACL (anon bez EXECUTE); bramka strukturalna: `get_or_create_direct_conversation`, `create_group_conversation`, `my_expert_request_quota` i `send_expert_request` przekazują tenanta JAWNIE. Powiadomienia: rodzaj `expert_request` w katalogu (CHECK + kolumna preferencji + gałąź CASE producenta), pełny cykl (nowe zapytanie → odbiorca, przyjęcie/odrzucenie → nadawca, wycofanie → odbiorca), dwujęzyczność, `href` z identyfikatorem i fragmentem statusu (dedup 5-minutowy nie zjada drugiego zdarzenia w wątku), stempel tenanta odbiorcy, zapis bez zmiany statusu bez powiadomienia oraz realne tłumienie przez `enabled_expert_request`. |
| `network_event_notifications_test.sql` | Pięć zdarzeń sieciowych przestaje milczeć (migracja 20260807140000). Stan zastany: wprowadzenia, rekomendacje, endorsementy, odsłony profilu i rezerwacje spotkań zapisywały wiersz i nie emitowały ŻADNEGO powiadomienia - cała rura doręczeń (`enqueue_notification` → in-app + web push + digest) działała od 20260710152630, brakowało wyłącznie PRODUCENTÓW. Plik przybija: katalog rodzajów (5 nowych wpisów w `notifications_kind_check` ORAZ bliźniacza kolumna `enabled_<rodzaj>` - oba sprawdzenia sterowane katalogiem, więc rodzaj bez kolumny wywala test bez dopisywania asercji); realne tłumienie każdego z pięciu rodzajów własną flagą (rodzaj bez gałęzi w CASE trafia w `ELSE true` i przecieka) plus dowód odwrotny (włączenie przepuszcza); wprowadzenia (INSERT → most, `forwarded` → cel ORAZ proszący z nazwą mostu w treści, `withdrawn` → CISZA); rekomendacje (`pending` → odbiorca do moderacji, `published` → autor z href-em na profil, na którym stoi tekst, `hidden` → CISZA); endorsementy (nazwa umiejętności w treści, `href` z `skill_id` chroni drugie potwierdzenie od dedupu 5-minutowego, wycofanie milczy); odsłony profilu jako skan CYKLICZNY ze znakiem wodnym (`run_profile_view_alerts` → jeden zbiorczy sygnał z LICZBĄ odsłon, stempel `profile_view_alert_state`, drugi przebieg w tej samej dobie milczy); spotkania 1-1 w dedykowanym rodzaju `meeting` zamiast ogólnego `content`, z nazwą rezerwującego i powiadomieniem hosta o zwolnionym slocie; ACL (`profile_view_alert_state` bez grantów klienckich, `run_profile_view_alerts` tylko service_role). |
| `profile_intent_semantic_test.sql` | Warstwa intencji, kompletność profilu i semantyka katalogu osób (migracje 20260807141000 / 20260807142000 / 20260807143000 / 20260807144000). Katalog intencji: CHECK odsiewa kod spoza `nes_profile_open_to_catalog()`, sufit 6 pozycji egzekwowany. Kompletność: `nes_profile_completeness_row` daje 0 dla pustego i 100 dla pełnego wiersza (wagi sumują się do 100), progi jakościowe odcinają krótkie bio / krótką intencję / 2 umiejętności, trigger UTRZYMUJE `profiles.completeness_score` i NADPISUJE wartość podaną przez klienta (kolumna jest pochodną, nie polem edycji), a zmiana w tabeli DZIECI (trzecia umiejętność) odświeża wynik. `intent_updated_at` stempluje się przy zmianie intencji i NIE przy dowolnej edycji profilu. `discovery_search` v3 wciąga tekst intencji, więc „konsorcjum Horizon" znajduje osobę bez warstwy wektorowej. `people_filter_options` zwraca fasetę `open_to` z licznikami; `search_people` v3 filtruje po `p_open_to` (intencja bez trafień = pusty zbiór, nie cały katalog), niesie intencję i kompletność w projekcji i degraduje do czystego trigramu bez wektora zapytania. Kolejka wektorów: `profiles_needing_embeddings` pomija profil poniżej progu kompletności, tekst źródłowy zawiera intencję, `prune_profile_embeddings` zabiera wektor po opt-oucie z katalogu, ACL (klient bez SELECT na `profile_embeddings`, bez EXECUTE na kolejce; `semantic_search_profiles` dostępne dla zalogowanego). Pętla zwrotna sugestii: `dismiss_connection_suggestion` usuwa osobę z `connection_suggestions`, licznik zgadza się z tabelą, `restore_connection_suggestions` przywraca. Stopień sieci jako JAWNA kolumna: 1 dla połączenia, 2 przy wspólnym kontakcie, 3 bez ścieżki - w `connection_statuses` i `connection_suggestions`. Alerty zapisanych wyszukiwań dla encji `people`: profil ponad znakiem wodnym wysyła sygnał mówiący o OSOBACH, drugi przebieg milczy. |
| `job_scheduler_heartbeat_test.sql`     | Harmonogram doręczeń (20260731110000): log przebiegów `job_runner_runs` service-role-only (RLS bez polityk, zero grantów klienckich), funkcje harmonogramu (`record_job_run`, `arm_job_runner`, `invoke_billing_cron`, `resolve_job_runner_base_url`) niedostępne dla anon/authenticated, `job_scheduler_health()` z bramką roli (członek → 42501, admin → pełny payload BEZ sekretu, z telemetrią `community_cron`), normalizacja wejścia w `record_job_run` (nieznane źródło → `external`, puste job → `all`, ujemny czas → 0) i heartbeat z licznikiem porażek, samozbrojenie `arm_job_runner` (tylko https bez hosta lokalnego, tylko dziewiczy wiersz, decyzja operatora nienaruszalna), `invoke_jobs_tick()` fail-open bez pg_net. |
| `community_cron_schedule_test.sql`     | Siatka społeczności w bazie (20260731210000): `invoke_community_cron`/`job_runner_autoarm` service-role-only, samozbrojenie WYŁĄCZNIE dziewiczego wiersza (raz; stempel i ręczne wyłączenie nienaruszalne), fail-open bez pg_net z jawnym powodem (`pg_net_unavailable`/`disabled`/`no_secret`) we WŁASNEJ telemetrii `community_last_tick_*` (bez dotykania telemetrii jobs-tick), job spoza kontraktu spada do `all`, wpis pg_cron `community-cron` co 5 minut w minutach 2,7,… (przeplot z oknem digestów jobs-tick). |
| `security_hardening_rls_test.sql`      | Bramka anti-regresyjna dla 5 findings ze skanu (2026-07-18): kolumnowy REVOKE PII/sekretów (`author_profiles.phone`, `author_profiles.media_contact_email`, `content_access.password_hash`, `content_access.password_hint_pl/en`) dla anon/authenticated; RLS `wp_import_jobs`/`domain_events`/`tenant_pending_counters` - reader (non-staff, non-actor) nie widzi nic, actor swoje, admin tenanta wszystko w tenancie; `profile_badges` public read tylko dla publicznego tenanta; `get_own_author_profile()` (SECURITY DEFINER) zwraca PII własnego wiersza. |
| `tenant_isolation_billing_storage_test.sql` | Regresja izolacji tenantów dla ten sam `auth.uid()` w dwóch tenantach: `billing_documents` i `donations` widzą tylko rekordy aktywnego tenanta (`user_id = auth.uid() AND tenant_id = current_tenant_id()`); storage bucket `cv` blokuje SELECT/DELETE plików spoza aktywnego tenanta pomimo zgodnego owner_id; test symetrii po przełączeniu profilu na drugi tenant. |
| `club_topics_tenant_isolation_test.sql` | Izolacja najemców w katalogu obszarów tematycznych klubów (migracja 20260816090000, finding „`club_topics_public_read` USING (true)"): polityka public read wiąże wiersz z `COALESCE(_caller_tenant(), public_tenant_id())` (pg_policies), RPC `club_topics_active()` filtruje po tym samym wyrażeniu w CIELE (SECURITY DEFINER omija RLS - stara wersja miała dla anon tautologię `ct.tenant_id = ct.tenant_id` i zwracała katalogi WSZYSTKICH najemców), anon widzi wyłącznie katalog najemcy z `x-tenant-host` (symetria obu domen; bez nagłówka - żadnego z najemców testowych), zalogowany wyłącznie katalog SWOJEGO tenanta, a podmiana nagłówka na cudzą domenę niczego nie otwiera - ani przez tabelę, ani przez RPC. Osobno przybity grant EXECUTE na `_caller_tenant()` dla ról klienckich (`has_function_privilege`): 20260718215759 odebrało go od PUBLIC bez re-grantu, a Postgres sprawdza EXECUTE przed wejściem w SECURITY DEFINER - bez grantu każda polityka wołająca funkcję (także `club_specializations_public_read` i `club_topics_admin_*`) rzucała `permission denied` zamiast filtrować. |
| `email_suppression_test.sql`           | Lista wykluczeń e-mail (bounce/complaint): twarde odbicie → blokada trwała + natychmiastowe wypisanie subskrybenta, miękkie → czasowa z backoffem i eskalacją do trwałej po 4 zdarzeniach, pierwszeństwo powagi (skarga nie słabnie od późniejszego soft bounce), `email_filter_suppressed`/`email_is_suppressed` tylko dla AKTYWNYCH blokad własnego tenanta (wygasłe i cudze nie blokują), idempotencja `email_apply_delivery_event` po `(provider, event_id)` + korelacja stanu dostawy po `provider_message_id`, RLS/PII (anon bez dostępu, nie-staff widzi zero) i bramka stafowa `email_suppression_add`. |
| `related_posts_config_provisioning_test.sql` | Provisioning i izolacja singletonu `related_posts_config`: backfill (każdy istniejący tenant ma wiersz), trigger `tenants_seed_related_posts_config` (nowy tenant zasiewa się sam), idempotencja `seed_related_posts_config` (bez duplikatu i bez nadpisania ustawień admina), upsert admina dotyka WYŁĄCZNIE wiersza jego tenanta, RLS blokuje zapis do wiersza obcego tenanta, oraz determinizm odczytu: goły `select().limit(1)` widzi DWA wiersze przy adminie obcego tenanta na cudzej domenie (suma polityk OR), a `get_related_posts_config()` zwraca zawsze wiersz tenanta PRZEGLĄDANEGO. |

## Uruchamianie

Kanonicznie - przez Supabase CLI, które stawia świeżą bazę, nakłada wszystkie
migracje z `supabase/migrations/` i odpala pliki przez `pg_prove`:

```bash
supabase test db          # albo: bun run db:test
```

Wymagane jest rozszerzenie pgTAP w lokalnej bazie testowej (jednorazowo):

```sql
create extension if not exists pgtap with schema extensions;
```

### Konwencje

- Każdy plik jest samowystarczalny: `begin; select plan(N); … select * from finish(); rollback;`.
  `ROLLBACK` na końcu nie zostawia żadnych danych - testy nie zależą od kolejności.
- Wcielanie się w użytkownika (RLS): `set local role authenticated|anon` +
  `set_config('request.jwt.claims', '{"sub":"…","role":"…"}', true)`; `auth.uid()`
  czyta `sub` z claims. Rolę zdejmujemy przez `reset role`.
- Seed nadaje `tenant_id`/role jawnie i wyłącza triggery rejestracji
  (`alter table auth.users disable trigger user`), żeby nie polegać na
  auto-provisioningu.

## Uwaga: środowisko bez Supabase CLI

`supabase test db` wymaga lokalnego stacka Supabase (Docker). W środowiskach bez
CLI te same pliki uruchomisz przez `pg_prove` na dowolnym Postgresie, który ma
nałożone migracje oraz schematy/role platformy Supabase (`auth.uid()`,
`auth.users`, role `anon`/`authenticated`/`service_role`, `storage.*`):

```bash
pg_prove -d "$DATABASE_URL" supabase/tests/*.sql
```

## Ujednolicenie listy wykluczeń i runner poczty

`email_suppression_unification_test.sql` - bramka anty-regresyjna dla migracji
`20260731120000_email_suppression_unification.sql`:

- `public.suppressed_emails` jest **widokiem** zgodności nad
  `public.email_suppressions` (relkind `v`), utworzonym z
  `security_invoker = true` i BEZ dostępu dla `anon`/`authenticated` - adres
  e-mail to PII, a widok bez `security_invoker` obszedłby RLS tabeli źródłowej.
- zapisy przez widok trafiają do listy kanonicznej przez
  `email_record_suppression` (mapowanie `bounce → hard_bounce`), są idempotentne
  i dziedziczą synchronizację z listą subskrybentów; `DELETE` **odblokowuje**
  (`released_at`) zamiast usuwać historię; blokada wygasła nie jest widoczna.
- `email_resolve_tenant_for_address`: jednoznaczny subskrybent → jednoznaczne
  konto → tenant domyślny (adres w dwóch tenantach NIE jest zgadywany).
- `email_unsubscribe_by_token`: wypis tokenem globalnym i tokenem per subskrybent
  w jednej transakcji (blokada + zdjęcie subskrypcji + zużycie tokenu),
  idempotentny przy powtórzonym one-click, z izolacją tenantów.
- runner zadań tła: `job_runner_settings.enabled` ma `DEFAULT true`, a
  `job_runner_base_url()` wylicza adres z domeny tenanta domyślnego, gdy
  konfiguracja jest pusta.

`share_full_article_budget_test.sql` - kontrakt mechaniki „Udostępnij pełny
artykuł" po migracji `20260806170000_share_full_article_click_budget.sql`:

- bramka `eligibility`: przy `registered` link generuje **każde konto tenanta**
  (anonim nie), przy `subscribers` wraca warunek płatnej subskrypcji;
- `create_gift_link` jest idempotentne per (wpis, nadawca), a limit miesięczny
  liczy **artykuły**, nie wiersze linków;
- treść bez paywalla i treść na hasło są wykluczone (`gift_post_not_gated`);
- budżet kliknięć: N różnych odbiorców dostaje body, kolejny `reason='exhausted'`
  **bez body**; powrót tego samego odbiorcy podbija `hits`, nie zużywa slotu;
- nadawca (`owner`) i czytelnik z własnym uprawnieniem (`entitled`) nie zużywają
  budżetu; `revoked` / `expired` / `invalid` mają własne powody;
- budżet jest **zamrożony na linku** - zmiana ustawień tenanta nie rusza linków
  już udostępnionych;
- rejestr `post_gift_redemptions` jest niewidoczny dla anona i zwykłego konta
  (tożsamość odbiorcy nie wraca do nadawcy).
